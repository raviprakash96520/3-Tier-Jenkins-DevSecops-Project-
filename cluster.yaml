apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: expense
  region: ap-south-1

managedNodeGroups:
  - name: spot-workers
    minSize: 2
    maxSize: 10
    desiredCapacity: 3
    spot: true
    instanceTypes:
      - m5.large
      - m5a.large
      - t3.large